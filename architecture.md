# 游戏开发需求与技术架构文档

1. 核心概述
   作品名称：拼好你的人生

产物形态：纯离线 HTML5 Canvas 互动 Web App（多文件目录结构）。

游玩设备：移动端触屏设备（竖屏锁定自适应）。

美术与情感基调：像素复古、治愈系、电影感叙事。

核心玩法：带有物理预判和时机抓取（Timing）机制的动态拼图。

2. 游戏流程与核心交互

2.1 整体叙事流转 (State Machine)
引导大厅：展示一面带有 4 个画框的墙壁，画框内为模糊处理的图画。提示玩家“点击画面开始”或点击第一幅画框。

入场动画：点击画框后，触发虚拟镜头推进（Zoom In）。画面平滑放大并聚焦于该画作的拼图区域，区域外保持模糊。

游玩阶段：挖空区域显示边框和底图的模糊滤镜。被挖出的拼图碎片在屏幕上方不同高度的轨道上，以不同速度向左或向右循环移动。

单局结算：所有图块归位后，清除提示边框。触发镜头拉远（Zoom Out），缩放回画框视图。图像由模糊平滑过渡至高清，并在上方浮现一段优美的文案。稍作停留后，提示玩家点击下一张。

终局动画：4 幅画作全部完成，触发全屏幻灯片（Slideshow）依次播放清晰画作与文案。随后画面渐黑（Fade to Black），屏幕中央浮现最终结局文字。

2.2 核心操作与判定
操作方式：单指点击/Tap 屏幕上方正在移动的拼图碎片。

下落机制：被点击的碎片停止水平移动，开始加速垂直下落。

层级判定：当多个碎片在移动轨道上重叠时，玩家点击屏幕，系统优先判定触碰到了 Z 轴层级最高（最上层）的碎片。

吸附与容错：碎片下落到目标高度时，系统计算其当前 X 坐标与目标 X 坐标的差值。若差值小于预设的 snapTolerance（容错距离），碎片自动吸附对齐到正确位置，并触发状态变更；若大于容错距离，碎片直接掉落出屏幕底部，经过一段延迟后在上方轨道重新生成。

3. 拼图切割与预处理引擎 (针对局部特征优化)

   为了适配每张底图里不同位置、不同比例的核心元素，预处理引擎必须完全受控于关卡配置（Config），具备高度的空间定位能力。

3.1 动态定位与预处理管线 (Pipeline)
百分比坐标映射：预处理不再局限于画面居中。引擎将读取当前关卡的 cutoutBoundary 数据。该数据采用相对百分比（例如 x: 0.1, y: 0.1 代表从左上角 10% 处开始），确保在任何尺寸的手机屏幕上，都能精准锁定对应的物体。

基底与模糊生成：

生成全屏高清基底 Canvas_Full（按屏幕比例 Cover 缩放）。

生成全图模糊的 Canvas_Blur。

局部视口锁定 (Viewport)：引擎根据坐标映射，在高清基底上圈定出目标对象的矩形“边界盒（Bounding Box）”（例如精确框住右下角的栏杆和街道）。在这个边界盒内，保留高清画面，边界盒外部则叠加模糊图层和提示边框。

3.2 自适应的形状与网格切分
动态拉伸网格：因为目标物体的宽高比各不相同（例如“大桥”是高瘦的长方形，“街道栏杆”则是宽扁的长方形），传统的正方形网格不再适用。引擎会根据边界盒的实际宽高，动态拉伸预定义的“凹凸拼图模板”，使其完美贴合该区域的比例。

动态行列配置：针对特别狭长或宽扁的区域，Config 中可以灵活配置不同的网格数（例如大桥区域可以切分成 2列 x 4行，而房子可以是 3列 x 3行）。

碎片独立渲染：在局部视口内，利用拉伸适配后的凹凸路径依次执行 ctx.clip()，提取图像、添加半透明轮廓描边，最终转化为独立的拼图碎片对象存入内存。

4. 全局参数字典 (Config)
   所有数值、文案、概率均禁止硬编码，统一维护在 config.js 中。

系统与物理配置：帧率 (fps)、吸附容错距离 (snapTolerance)、碎片下落重力加速度。

生成与移动规则：移动速度随机区间 (min, max)、方向生成概率、掉落未命中后的重生延迟、上方移动轨道的 Y 轴高度集合。

动画过渡时长：模糊渐变时间、镜头 Zoom In/Out 时间、文案淡入淡出时间。

关卡剧本数据 (Array)：包含每关的底图路径、画框路径、挖空区域相对比例（自适应宽高的百分比）、网格划分（如 3x3）、切割模板名称（如 "classic_jigsaw"）、单关通关文案。

终局剧本数据：幻灯片轮播间隔、最终致谢文案。

UI 与文案：错误捕获提示语、引导文案等。

5. 目录结构与技术栈
   遵循单一职责原则与纯原生开发约束。

5.1 目录结构
Plaintext
sunset-snap/
├── index.html  
├── css/
│ └── style.css  
├── js/
│ ├── config.js  
│ ├── core.js  
│ ├── renderer.js  
│ ├── input.js  
│ └── templates.js  
└── assets/
└── images/  
5.2 技术栈限制
核心渲染：HTML5 Canvas 2D API。

主循环：requestAnimationFrame。

本地存储：localStorage（用于记录当前关卡进度，断点续玩）。

环境依赖：零依赖，完全依靠原生 JavaScript (ES6+)。不使用任何第三方框架、网络请求库或外部 CDN。

6. 产物交付与合规约束
   离线运行：所有资源（包含脚本、样式、图片）务必存储于项目目录内，绝对禁止使用 fetch、XMLHttpRequest 等网络请求 API。

体积限制：最终产物目录打包为 ZIP 格式后，总大小严格控制在 8MB 以内。

UI 适配：锁定竖屏显示。通过 CSS 和 Canvas 内置的按比例缩放计算，实现全机型自适应，杜绝横向或纵向原生滚动条的出现。

健壮性：全局关键流程使用 try-catch 包裹。触发异常时，向页面输出 Config 中配置的友好错误提示文案（如：“哎呀，出错了，请重启试试吧~”），并终止循环以防设备发热。
